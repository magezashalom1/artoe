<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ sneaker.name }}</title>
</head>

<body>
    <h1>{{ sneaker.name }}</h1>
    <p>Price: ${{ sneaker.price }}</p>
    <p>Description: {{ sneaker.description }}</p>
    <a href="{% url 'sneaker_list' %}">Back to Sneaker List</a>
</body>
</html>